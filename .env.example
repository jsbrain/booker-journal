# Authentication
BETTER_AUTH_SECRET=your-secret-key-change-in-production
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database - PostgreSQL (Docker Compose)
POSTGRES_DB=booker_journal
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=35432

# Database URL (uses variable substitution - automatically synced with values above)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# Or use a hosted PostgreSQL service:
# DATABASE_URL=postgresql://user:password@host:5432/database
# For Vercel Postgres: postgres://user:password@region.postgres.vercel-storage.com:5432/database
# For Supabase: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
